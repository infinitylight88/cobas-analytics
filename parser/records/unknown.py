from parser.records.base_record import BaseRecord


class UnknownRecord(BaseRecord):

    def process(self, archive_id, raw_id, line_number):

        from parser.records.base_record import BaseRecord


class UnknownRecord(BaseRecord):

    def process(self, archive_id, raw_id, line_number):
        pass